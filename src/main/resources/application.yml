server:
  port: 8080
spring:
  application:
    name: rest-emulator
  profiles:
    active: DEV
  kafka:
    bootstrap-servers: localhost:9092
    topic: "messages"
    consumer-group: "messages-group"
  data:
    redis:
      host: localhost
      port: 6379
logging:
  level:
    .: INFO
